{% extends 'base.html.twig' %}

{% block title %}{{ email_template.name }}{% endblock %}

{% block body %}
    <h1 style="color: red">Edit: "{{ email_template.name }}" template</h1>

    {{ include('email_templates/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('email_templates_index') }}">Return to list</a>

    {{ include('email_templates/_delete_form.html.twig') }}
{% endblock %}
