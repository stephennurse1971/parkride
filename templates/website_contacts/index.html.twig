{% extends 'base.html.twig' %}

{% block title %}Website Contacts{% endblock %}

{% block body %}
    <h1 style="color: red">Website Contacts</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Status</th>
            <th>Date</th>
            <th>Time</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>Service</th>
            <th>Notes</th>
            <th>Client already<br>in system?</th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for website_contact in website_contacts %}
            <tr>
                <td>
                    {{ website_contact.status }}
                </td>
                <td data-sort="{{ website_contact.dateTime|date('Y-m-d H:i') }}">{{ website_contact.dateTime ? website_contact.dateTime|date('d-M-y') : '' }}</td>
                <td>{{ website_contact.dateTime ? website_contact.dateTime|date('H:i') : '' }}</td>
                <td>
                    {{ website_contact.firstName }}
                </td>
                <td>
                    {{ website_contact.lastName }}
                </td>
                <td>
                    {% if website_contact.email is not null %}
                        <a href="mailto:{{ website_contact.email }}">
                            <i class="fa fa-envelope"></i>
                        </a>
                    {% endif %}
                    {{ website_contact.email }}
                </td>
                <td>
                    {% if website_contact.mobile  is not empty %}
                        <a target="_blank" href="https://wa.me/{{ website_contact.mobile   | replace({' ': ''}) }}">
                            <i title="{{ website_contact.mobile }}" class="fab fa-whatsapp"
                               style="color:green; text-align: center"></i></a>
                    {% endif %}

                    {{ website_contact.mobile }}
                </td>
                <td style="text-align: left">{{ website_contact.service.serviceOffered }}</td>
                <td style="text-align: left">{{ website_contact.notes }}</td>
                <td style="text-align: center">
                    {% for user in users %}
                        {% if user.email == website_contact.email %}
                            {% set website_contact_fullName = user.fullName %}
                            <a target="_blank" title="Open client's transaction history"
                               href="{{ path('transaction_index', {'clientName': website_contact_fullName, 'subset': 'All'} ) }}">
                                <i style="color:green" class="fa fa-user-alt-slash"></i></a>
                            {% if website_contact.firstName != user.firstName  or  website_contact.lastName != user.lastName
                                or website_contact.mobile != user.mobile %}
                                <i style="color: yellow; background-color: red" class="fa fa-warning"
                                   title="First Name,  Last Name or Mobile don't match vs. client data"></i>
                                <i style="color: grey" class="fa fa-info-circle"
                                   title="First Name: {{ user.firstName }};  Last Name: {{ user.lastName }};  Mobile: {{ user.mobile }}"></i>
                            {% endif %}
                        {% endif %}
                    {% endfor %}


                </td>
                <td>
                    <a class="btn btn-outline-danger btn-sm"
                       href="{{ path('website_contacts_edit', {'id': website_contact.id}) }}">Edit</a>
                </td>
                <td>
                    <a class="btn btn-success btn-sm"
                       href="{{ path('user_new_from_website_contacts', {'websiteContactId': website_contact.id}) }}">Convert
                        to User</a>
                </td>
                <td>
                    {% if website_contact.status == 'Pending' %}
                    <a class="btn btn-outline-danger btn-sm"
                       href="{{ path('website_contacts_set_to_junk', {'id': website_contact.id}) }}">Spam</a>
                    {% endif  %}
                    {% if website_contact.status == 'Junk' %}
                        <a class="btn btn-outline-primary btn-sm"
                           href="{{ path('website_contacts_revert_to_pending', {'id': website_contact.id}) }}">Revert to Pending</a>
                    {% endif  %}

                </td>

                <td>
                    <form method="post" action="{{ path('website_contacts_delete', {'id': website_contact.id}) }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ website_contact.id) }}">
                        <button class="btn btn-danger btn-sm">
                            <i class="fa fa-trash"></i>
                        </button>
                    </form>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success btn-sm" href="{{ path('website_contacts_new') }}">New</a>
{% endblock %}


{% block datatable %}
    <script>
        $(document).ready(function () {
            $('.table').DataTable({
                'pageLength': 100,
                "order": [[0, 'asc']],
                "paging": true,
                "searching": true,
                "bInfo": true
            });
        });
    </script>
{% endblock datatable %}
