<form method="post" action="{{ path('client_availability_delete', {'id': client_availability.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_availability.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
