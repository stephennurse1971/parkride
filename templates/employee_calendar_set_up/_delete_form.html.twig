<form method="post" action="{{ path('employee_calendar_set_up_delete', {'id': employee_calendar_set_up.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_calendar_set_up.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
