{% extends 'base.html.twig' %}

{% block title %}Edit Emails{% endblock %}

{% block body %}
    <h1 style="color: red">Edit Emails</h1>

    {{ include('emails/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('emails_index') }}">Return to list</a>

    {{ include('emails/_delete_form.html.twig') }}
{% endblock %}
