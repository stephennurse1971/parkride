{% extends 'base.html.twig' %}

{% block title %}Guidelines: {{ document_guideline.document }} {% endblock %}

{% block body %}
    <h1 style="color: red">Guidelines: {{ document_guideline.document }}</h1>

    {{ include('document_guidelines/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('document_guidelines_index') }}">Return to list</a>

    {{ include('document_guidelines/_delete_form.html.twig') }}
{% endblock %}
