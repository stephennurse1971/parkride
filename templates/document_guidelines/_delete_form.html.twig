<form method="post" action="{{ path('document_guidelines_delete', {'id': document_guideline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_guideline.id) }}">
    <button class="btn btn-danger bt-sm">Delete</button>
</form>
