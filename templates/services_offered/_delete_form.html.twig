<form method="post" action="{{ path('services_offered_delete', {'id': services_offered.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ services_offered.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
