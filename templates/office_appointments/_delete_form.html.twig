<form method="post" action="{{ path('office_appointments_delete', {'id': office_appointment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ office_appointment.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
